# PostGIS connection (set to enable the persistent repository).
# When unset, the API falls back to an in-memory store (non-persistent).
DATABASE_URL=postgres://projectwatch:projectwatch@localhost:5433/projectwatch

# Optional: override the ingestion/object-storage endpoints (Phase 2+).
# MINIO_ENDPOINT=http://localhost:9000
